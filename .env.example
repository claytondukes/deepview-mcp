# DeepView MCP Environment Variables

# Gemini API Configuration
# Get your API key from: https://aistudio.google.com/
GEMINI_API_KEY=your_api_key_here
GEMINI_MODEL=gemini-2.5-flash

# Server Configuration
MCP_PORT=8019
MCP_HOST=0.0.0.0
MCP_TRANSPORT=http

# Docker Configuration
CONTAINER_NAME=deepview-mcp
NETWORK_NAME=deepview-mcp-network

# Volume Mounts (adjust paths as needed)
CODEBASE_PATH=./codebase
LOGS_PATH=./logs
DATA_PATH=./data

# Logging Configuration
LOG_LEVEL=INFO
PYTHONUNBUFFERED=1

# Health Check Configuration
HEALTH_CHECK_INTERVAL=30s
HEALTH_CHECK_TIMEOUT=10s
HEALTH_CHECK_RETRIES=3
HEALTH_CHECK_START_PERIOD=60s

# OAuth / OIDC (Auth0) Configuration
# Toggle OAuth (enable for ChatGPT, disable for local Windsurf)
OAUTH_ENABLED=false
# Auth0 issuer (domain). Include scheme and no path. Trailing slash optional.
OIDC_ISSUER=https://YOUR_TENANT.us.auth0.com/
# API Identifier (Audience) configured in Auth0 API settings
OIDC_AUDIENCE=https://mcp.example.com/api
# Optional: override JWKS URL; otherwise discovered from issuer
# OIDC_JWKS_URL=https://YOUR_TENANT.us.auth0.com/.well-known/jwks.json
# Algorithms accepted
OIDC_ALGS=RS256
# Global scopes required (space/comma separated). For now only deepview:read
OAUTH_REQUIRED_SCOPES=deepview:read
# Clock skew leeway in seconds
OAUTH_CLOCK_SKEW_SECONDS=60

# Auth0 Client Credentials (for token helper script)
# DO NOT COMMIT REAL VALUES
AUTH0_CLIENT_ID=
AUTH0_CLIENT_SECRET=
# Optional: override token URL (defaults to ${OIDC_ISSUER%/}/oauth/token)
# AUTH0_TOKEN_URL=
